# Samples

This folder contains a (incomplete) set of samples from the OpenCV samples directory, rewritten to use the pipline.
This serves both as a test set, and example usage of the pipeline.

```bash
[...]/opencv/3.4/source/samples
```

